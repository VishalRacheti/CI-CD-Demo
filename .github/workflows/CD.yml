name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI"]   # 👈 must match CI name
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy Application
        run: echo "🚀 Deploying because CI passed"
